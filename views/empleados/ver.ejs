<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #000;
            color: #fff;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            background: #111;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 170, 255, 0.4);
            margin: 20px auto;
        }
        h2 {
            color: #00aaff;
            text-align: center;
            margin-bottom: 20px;
        }
        .empleado-details {
            background: #222;
            padding: 20px;
            border-radius: 5px;
            margin-top: 20px;
        }
        .detail-row {
            display: flex;
            margin-bottom: 20px;
        }
        .detail-label {
            font-weight: bold;
            width: 150px;
            color: #00aaff;
        }
        .detail-value {
            margin-left: 20px;
        }
        .actions {
            margin-top: 20px;
            text-align: center;
        }
        .btn {
            padding: 12px 20px;
            text-decoration: none;
            border-radius: 6px;
            margin: 5px;
            cursor: pointer;
            font-size: 16px;
            border: none;
            transition: background 0.3s, transform 0.2s;
        }
        .btn-primary {
            background-color: #00aaff;
            color: #000;
        }
        .btn-primary:hover {
            background-color: #0088cc;
            transform: scale(1.05);
        }
        .btn-secondary {
            background-color: #555;
            color: white;
        }
        .btn-secondary:hover {
            background-color: #777;
            transform: scale(1.05);
        }
        .btn:hover {
            opacity: 0.9;
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>

    <main class="container">
        <div style="display: flex; justify-content: space-between; align-items: center;">
            <h2>Detalles del Empleado</h2>
        </div>

        <% if (typeof success !== 'undefined') { %>
            <div style="color: green; margin: 10px 0;"><%= success %></div>
        <% } %>

        <% if (typeof error !== 'undefined') { %>
            <div style="color: red; margin: 10px 0;"><%= error %></div>
        <% } %>

        <div class="empleado-details">
            <div class="detail-row">
                <div class="detail-label">ID:</div>
                <div class="detail-value"><%= empleado.id_empleado %></div>
            </div>
            <div class="detail-row">
                <div class="detail-label">Nombre:</div>
                <div class="detail-value"><%= empleado.nombre %></div>
            </div>
            <div class="detail-row">
                <div class="detail-label">Apellido:</div>
                <div class="detail-value"><%= empleado.apellido %></div>
            </div>
            <div class="detail-row">
                <div class="detail-label">Cédula:</div>
                <div class="detail-value"><%= empleado.cedula %></div>
            </div>
            <div class="detail-row">
                <div class="detail-label">Teléfono:</div>
                <div class="detail-value"><%= empleado.telefono %></div>
            </div>
            <div class="detail-row">
                <div class="detail-label">Departamento:</div>
                <div class="detail-value"><%= empleado.departamento %></div>
            </div>
            <div class="detail-row">
                <div class="detail-label">Especialidad:</div>
                <div class="detail-value"><%= empleado.especialidad %></div>
            </div>
            <div class="detail-row">
                <div class="detail-label">Email:</div>
                <div class="detail-value"><%= empleado.email %></div>
            </div>
            <div class="detail-row">
                <div class="detail-label">Rol:</div>
                <div class="detail-value"><%= empleado.rol %></div>
            </div>
            <div class="detail-row">
                <div class="detail-label">Nombre de usuario:</div>
                <div class="detail-value"><%= empleado.username %></div>
            </div>
        </div>

        <div class="actions">
            <a href="/empleados/editar/<%= empleado.id_empleado %>" class="btn btn-primary">Editar</a>
            <a href="/empleados" class="btn btn-secondary">Volver</a>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>

