<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Tecno-Fix</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #000;
            color: #fff;
        }

        ::selection {
            background-color: #007bff;
            color: white;
        }

        .active-link {
            color: #007bff !important;
            font-weight: bold;
        }

        .navbar-brand, .nav-link, .navbar-text {
            color: #fff !important;
        }

        .navbar {
            border-bottom: 1px solid #333;
        }

        .navbar-text {
            margin-top: 0;
            margin-left: 15px; 
        }

        .nav-item {
            display: flex;
            align-items: center;
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="/" class="text-primary">Tecno-Fix</a> 
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto">
                        <% if (locals.user) { %>
                            <% if (locals.user.rol === 'Cliente') { %>
                                <li class="nav-item">
                                    <a href="/solicitar-soporte" class="nav-link <%= currentPage === 'solicitar-soporte' ? 'active-link' : '' %>">Solicitar Soporte</a>
                                </li>
                            <% } %>
                            <% if (locals.user.rol === 'Administrador') { %>
                                <li class="nav-item">
                                    <a href="/empleados" class="nav-link <%= currentPage === 'empleados' ? 'active-link' : '' %>">Empleados</a>
                                </li>
                                <li class="nav-item">
                                    <a href="/inventarios" class="nav-link <%= currentPage === 'inventarios' ? 'active-link' : '' %>">Inventarios</a>
                                </li>
                            <% } %>
                        <% } %>
                    </ul>
                    <ul class="navbar-nav">
                        <% if (locals.user) { %>
                            <% if (locals.user.nombre && locals.user.apellido) { %>
                                <li class="nav-item">
                                    <span class="navbar-text me-3">Bienvenido <%= locals.user.nombre %> <%= locals.user.apellido %></span>
                                </li>
                            <% } %>
                            <li class="nav-item">
                                <a href="/logout" class="nav-link">Cerrar sesión</a>
                            </li>
                        <% } else { %>
                            <li class="nav-item">
                                <a href="/registro" class="nav-link <%= currentPage === 'registro' ? 'active-link' : '' %>">Registrarse</a>
                            </li>
                            <li class="nav-item">
                                <a href="/login" class="nav-link <%= currentPage === 'login' ? 'active-link' : '' %>">Iniciar sesión</a>
                            </li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
